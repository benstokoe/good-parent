# GoodParent (Expo)

An Expo/React Native port of GoodParent — see `../GoodParent` for the original SwiftUI/macOS app. See `CLAUDE.md` for context and conventions.

Scaffolded with `create-expo-app` on Expo SDK 54 (Expo Router, TypeScript). Run `npx expo start` and scan the QR code with Expo Go.
